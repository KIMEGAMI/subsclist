SubscList AGENTS.md

1. 目的

このファイルは、CodexがSubscListを安全に調査・実装・修正するための必須ルールを定める。

最優先事項は次のとおり。

既存機能を壊さない。

指示されていない変更をしない。

現在のソースコードとテストを事実として扱う。

テスト、ビルド、差分レビューが終わるまで完了としない。

秘密情報とユーザーデータを保護する。

個別タスクの明示的な指示とこのファイルが競合する場合は、個別タスクの指示を優先する。ただし、データ消失、秘密情報漏えい、本番停止につながる場合は、実行前にユーザーへ確認する。

2. プロジェクト情報

プロジェクト名：SubscList（サブスクリスト）

目的：サブスクリプション契約、支出、更新日などを管理する公開可能なSaaS

開発環境：C:\MyDeveloper\SubscList

本番環境：/var/www/subsclist

公開URL：https://subsclist.shinji.work

フロントエンド／サーバー：Next.js App Router

言語：TypeScript

UI：Tailwind CSS

データベース：MySQL

ORM：Prisma

認証：メール認証、Google OAuth

課金：Stripe

本番プロセス管理：PM2

Webサーバー：Apache

ソース管理：GitHub

バージョン、認証ライブラリ、ディレクトリ構成、環境変数名、実行コマンドは変更される可能性がある。必ず現在のpackage.json、ロックファイル、Prismaスキーマ、設定ファイル、ソースコードを確認すること。

設計書や過去の説明に記載されているだけでは、実装済みとは判断しない。現在のソースコード、ルート、画面、テスト、DBスキーマで実装状態を確認すること。

3. サービス上の重要仕様

既知の基本プランは次のとおり。

Free：無料

Premium：月額480円

既存コードに別の有効なプランや買い切り処理が存在する場合は、勝手に削除しない。料金、件数制限、利用可能機能、Stripe Price IDの対応を推測で変更しない。

既知の主要機能領域：

メール認証

Googleログイン

サブスクリプション管理

カテゴリ管理

支払い管理

ダッシュボードと集計

Analytics

月次・年間レポート

通知管理

CSV入出力

AI分析

解約支援

Stripe課金

設定管理

この一覧は実装を保証するものではない。各タスク開始時に、対象機能が実装済み、部分実装、未実装のどれかをソースから判定すること。

4. 作業開始前の必須確認

編集前に次を行う。

リポジトリルートと現在の作業ディレクトリを確認する。

git status --shortで既存変更を確認する。

ユーザーの未コミット変更を特定し、上書きしない。

package.jsonとロックファイルを確認する。

Prismaスキーマと既存マイグレーションを確認する。

対象機能の画面、Route Handler、Service、認証、DB、テストを検索する。

呼び出し元と呼び出し先を確認する。

対象ファイルを最初から最後まで読む。

変更前の正常動作と問題を区別する。

可能なら変更前の関連テストを実行する。

検索には可能な限りrgまたはrg --filesを使用する。

調査結果では、確認できた事実と推測を分ける。不明点によって実装結果が大きく変わる場合は、推測で進めず質問する。

5. 高リスク変更は先に計画する

次の変更は、原則として実装前に計画を提示する。

メール認証、Google OAuth、セッション、Cookie

Stripe Checkout、Webhook、料金、プラン判定

Prismaスキーマ、マイグレーション、既存データ

ユーザー権限、管理者権限、データ所有者判定

外部API、AI API、メール送信

公開URL、リダイレクトURL、環境変数

Apache、PM2、Node.js、本番デプロイ

新しい依存パッケージ

複数機能にまたがる変更

公開APIや画面遷移の互換性を変える変更

計画には次を記載する。

確認できた事実

不明点と仮定

原因または実装方針

変更予定ファイル

既存機能への影響

テスト方法

セキュリティと課金への影響

元に戻す方法

6. 変更範囲

1タスクでは1機能または1不具合を扱う。

目的達成に必要な最小範囲だけ変更する。

ファイル全体を読んでから、必要箇所だけ編集する。

小さな修正のためにファイル全体を書き直さない。

無関係なリファクタリング、整形、改名、移動をしない。

既存機能を削除または簡略化しない。

仮ページ、ダミー処理、見せかけのボタンを作らない。

UI、文言、色、余白、ナビゲーションを勝手に変えない。

既存のルート、APIレスポンス、DB列、認証方式を勝手に変えない。

依存パッケージを無断で追加、削除、更新しない。

未実装機能を実装済みと報告しない。

指示にない機能を「便利だから」という理由で追加しない。

7. TypeScript・Next.js

TypeScriptの型エラーを残さない。

安易にany、型アサーション、@ts-ignoreを使わない。

APIリクエストとレスポンスに型を付ける。

Server ComponentとClient Componentの境界を確認する。

Route HandlerのHTTPメソッドとレスポンス形式を維持する。

サーバー専用の秘密情報をクライアントへ渡さない。

NEXT_PUBLIC_には公開してよい値だけを設定する。

null、undefined、空データ、通信失敗を考慮する。

ローディング、成功、入力エラー、通信エラーを処理する。

保存・決済・メール送信などの二重送信を防ぐ。

既存のレスポンシブ表示を維持する。

ブラウザコンソールに新しいエラーや警告を残さない。

lintや型チェックを無効化して通過させない。

package.jsonに存在しないスクリプトを推測で実行しない。

8. 認証・ユーザー分離

現在の認証実装を確認し、Auth.jsやNextAuthへ勝手に置き換えない。

メール認証とGoogle OAuthの両方を維持する。

認証済みであることだけを認可条件にしない。

更新・参照・削除では対象データのuserIdまたは所有者を必ず確認する。

URLのIDを書き換えて他ユーザーのデータへアクセスできないようにする。

Free、Premium、管理者の権限判定をサーバー側でも行う。

クライアント側の非表示だけで権限制御を完了しない。

セッション固定、CSRF、オープンリダイレクトを防ぐ。

認証エラーにより、ユーザーの存在や内部情報を漏らさない。

認証変更後に最低限確認する。

メールログイン

Googleログイン

ログアウト

未認証メール

未ログインアクセス

セッション切れ

正常なリダイレクト

不正なstate

他ユーザーのデータへのアクセス拒否

FreeとPremiumの権限制御

9. Google OAuth

Google Cloud Consoleの設定とアプリのリダイレクトURLを完全一致させる。

開発URLと本番URLを混在させない。

httpとhttps、ホスト名、ポート、パス、末尾スラッシュを確認する。

URLを連結する処理ではhttps://https://のような重複を発生させない。

OAuthのstateを生成・保存・検証する。

state Cookieの有効期限、Secure、HttpOnly、SameSiteを確認する。

Client Secretをクライアントへ送らない。

OAuthエラー時にSecret、token、内部レスポンスを表示しない。

環境変数の変更後は、必要な再ビルド・再起動を明記する。

Google OAuthを変更する場合は、開始ルート、コールバック、Cookie、ユーザー作成・紐付け、リダイレクト先を一連の処理として確認する。

10. Stripe・課金

Stripeのテストキーと本番キーを混在させない。

Price ID、Webhook Secret、公開鍵、秘密鍵の環境対応を確認する。

月額480円という仕様を勝手に変更しない。

Stripe上のPriceとアプリ内プラン判定の対応を確認する。

金額やPremium権限をクライアントから受け取った値だけで確定しない。

Checkout成功画面を表示しただけでPremium化しない。

Webhook署名を必ず検証する。

Webhook処理は二重配信に耐えられるよう冪等にする。

Stripe Event IDなどを利用し、同一イベントを二重処理しない。

対応していないイベントを成功処理したことにしない。

解約、支払い失敗、返金、期限切れの状態を考慮する。

APIキー、Webhook Secret、顧客情報をログへ出力しない。

実課金やStripe設定変更は、明示的な許可なしに行わない。

課金変更後に最低限確認する。

Checkout Session作成

不正なプラン指定の拒否

Webhook署名不正の拒否

Webhook再送時の二重処理防止

Premium反映

支払い失敗

解約後の権限

Freeユーザーの制限

Premiumユーザーの機能

11. Prisma・MySQL

現在使用しているPrismaスキーマの場所を確認する。

スキーマが見つからない場合は、別の場所に新規作成せず原因を調査する。

Prisma Clientを変更後に必要なら再生成する。

既存データを失う変更をしない。

本番適用済みマイグレーションを安易に書き換えない。

スキーマ変更は新しいマイグレーションで管理する。

必須カラム追加時は既存行への対応を設計する。

一意制約、外部キー、削除時動作を確認する。

全てのユーザーデータ取得で所有者条件を確認する。

接続プール不足やタイムアウトを、接続数の無制限増加だけで解決しない。

Prisma Clientの多重生成や開発時の接続増加を確認する。

明示的な許可なしに次を実行しない。

prisma migrate reset
prisma db push --force-reset
DROP DATABASE
DROP TABLE
TRUNCATE TABLE
本番データの一括削除

本番では原則として、確認済みマイグレーションをprisma migrate deployで適用する。prisma migrate devや破壊的なdb pushを本番で使用しない。

12. Free・Premium制御

現在の実装と仕様書を照合し、実際の制限値を確認する。

既知の基準：

Free：サブスクリプション登録10件まで

Free：カテゴリ5件まで

Free：基本通知

Premium：登録数・カテゴリ数の上限拡張または無制限

Premium：CSV、高度分析、複数通知などの追加機能

制限は画面側だけでなく、Route Handlerやサーバー処理でも検証する。同時送信による上限超過を考慮する。料金プランや機能境界に不一致を見つけた場合は、勝手に統一せず報告する。

13. CSV・通知・AI

CSV：

ユーザーごとのデータだけを対象にする。

CSVインジェクションを考慮する。

文字コード、日付、金額、必須列、重複データを検証する。

インポート途中の失敗で不整合を残さない。

大量データによるメモリ消費を考慮する。

通知・メール：

同じ通知やメールの二重送信を防ぐ。

送信失敗を利用者データの破損につなげない。

宛先、認証情報、トークンをログへ出さない。

無制限送信を防ぐ。

AI分析：

APIキーをクライアントへ渡さない。

他ユーザーのデータをプロンプトへ混入させない。

必要以上の個人情報を外部APIへ送信しない。

API利用回数、タイムアウト、料金上限を考慮する。

AIの回答を確定的な事実として保存・表示しない。

AI障害時もサブスクリプション管理機能を利用可能にする。

14. 秘密情報

次を表示、記録、コミットしない。

.env、.env.local、本番環境変数

Database URL

Google Client Secret

Stripe Secret Key

Stripe Webhook Secret

SMTPパスワード

セッションSecret

AI API Key

Access Token、Refresh Token

ユーザーの個人情報

新しい環境変数が必要な場合は、.env.exampleに変数名と安全な説明だけを追加する。実値を記載しない。

秘密情報の漏えいを見つけた場合は、その値を再表示せず作業を止めて報告する。

15. テスト

不具合修正は可能な限り次の順で行う。

不具合を再現する。

再現テストを追加または特定する。

修正前に期待どおり失敗することを確認する。

最小限の修正を行う。

追加したテストを実行する。

関連する既存テストを実行する。

Prisma、型、lint、buildを確認する。

最終差分をレビューする。

package.jsonを確認し、存在するスクリプトを使用する。

基本的な確認候補：

npm ci
npx prisma validate
npx prisma generate
npm run lint
npx tsc --noEmit
npm test
npm run build

既存スクリプトがある場合は、そのコマンドを優先する。

テストのためだけに本番APIや実課金を実行しない。

正常な既存テストを削除、skip、弱体化して通過させない。

実行できなかった確認を成功扱いにしない。

変更前からの失敗と今回追加した失敗を区別する。

16. デグレード確認

変更内容に応じて次を確認する。

トップページ

メールログイン

Googleログイン

ログアウト

ダッシュボード

サブスク一覧・登録・詳細・編集・削除

カテゴリ管理

支払い管理

集計とレポート

通知

CSV入出力

AI分析

解約支援

Stripe Checkout

Premium反映

設定

Free／Premium制御

他ユーザーのデータ分離

PC表示

スマートフォン表示

全てを確認できない場合は、未確認項目と理由を明記する。

17. Git・デプロイ

本番環境でソースコードを直接修正しない。

Windows開発環境で修正する。

テストとビルド後にGitHubへpushする。

本番環境ではGitHubの変更をpullして反映する。

ユーザーの許可なくcommit、push、merge、deployしない。

ユーザーの未コミット変更を削除しない。

無関係な変更をコミットに含めない。

.env、ログ、ビルド生成物、秘密情報をコミットしない。

基本の反映経路：

C:\MyDeveloper\SubscList
  -> テスト・ビルド
  -> GitHub
  -> /var/www/subsclist
  -> Prisma migration
  -> 本番ビルド
  -> PM2再起動
  -> 動作確認

本番でコマンドを実行する前に、現在位置が/var/www/subsclistであることを確認する。/home/ubuntuなど別の場所でnpm、Prisma、Gitコマンドを実行しない。

本番反映時は、現在のデプロイ手順、PM2プロセス名、Node.jsバージョン、環境変数を確認する。ビルドが失敗した状態でPM2を再起動しない。

18. 禁止する操作

明示的な許可なしに次を行わない。

git reset --hard
git clean -fd
git checkout --
git restoreによるユーザー変更の破棄
prisma migrate reset
prisma db push --force-reset
本番DBの削除・初期化
本番環境の直接編集
本番環境変数の変更
Stripeの実課金
Stripe商品・Priceの作成や変更
Google OAuth設定の変更
APIキーの再発行・無効化
GitHubへのpush
本番deploy

必要な場合は、対象、影響、復旧方法、代替案を説明して確認を得る。

19. 差分レビュー

実装後はgit diffとgit diff --checkを確認し、次をレビューする。

依頼内容と実装が一致しているか。

無関係な変更がないか。

既存機能や画面を削除していないか。

認証と権限に問題がないか。

他ユーザーのデータへアクセスできないか。

FreeとPremiumの制御を壊していないか。

Stripeの金額やWebhook処理を壊していないか。

Google OAuthのURLやstate処理を壊していないか。

Prismaスキーマと既存データに危険がないか。

APIキーや個人情報を含んでいないか。

WindowsとLinuxの差で問題が起きないか。

API、画面遷移、UIに意図しない変更がないか。

テストが不足していないか。

問題を見つけた場合は修正し、影響する検証を再実行する。

20. 完了条件

次を満たすまで完了としない。

指示された動作を実装した。

既存機能を削除していない。

無関係な変更がない。

必要なテストを追加または更新した。

対象機能のテストが成功した。

関連する既存テストが成功した。

Prismaの検証が成功した。

TypeScript型チェックが成功した。

lintが成功した。

本番用buildが成功した。

最終差分をレビューした。

秘密情報を含んでいない。

未確認事項と残存リスクを報告した。

環境上の理由で実行できない項目は、完了報告で明記する。

21. 完了報告

完了時は次の形式で報告する。

実装結果

何を実装または修正したか。

原因

不具合の場合は確認できた原因。推測は推測と明記する。

変更ファイル

変更した全ファイルをフルパスで列挙し、変更理由を書く。

テスト結果

実行したコマンドと成功・失敗を書く。

デグレード確認

確認した既存機能と結果を書く。

未確認事項

実行できなかった確認と理由を書く。

残存リスク

手動確認が必要な項目を書く。

次の行動

ユーザーが次に行う安全な操作を書く。

22. 作業停止条件

次の場合は推測で進めず、ユーザーへ確認する。

要求に複数の意味がある。

現在の仕様と依頼内容が矛盾する。

ユーザーの未コミット変更と重なる。

DBデータを失う可能性がある。

認証方式を変える必要がある。

料金、プラン、Stripe設定を変える必要がある。

公開APIやルートの互換性を壊す必要がある。

新しい有料サービスや大きなAPI費用が発生する。

新しい本番依存パッケージが必要になる。

セキュリティを弱める必要がある。

秘密情報や権限が不足している。

テストまたはbuildが失敗したまま本番反映が必要になる。

確認時は、問題点、影響、推奨案、代替案を示す。

# 破壊的操作禁止・データ保護ルール

## 1. 基本方針

このプロジェクトでは、既存のソースコード、データベース、ユーザーデータ、設定ファイル、Git履歴を重要な資産として扱う。

実装を進めるために、既存データや既存機能を削除して問題を解決してはならない。

「開発環境だから削除してよい」「テストのためなら初期化してよい」「作り直した方が早い」という判断を、Codexが独自に行うことを禁止する。

既存データとの不整合や実装上の問題が発生した場合は、削除や初期化ではなく、原因を調査し、既存データを保持したまま修正すること。

---

## 2. 絶対に禁止する操作

ユーザーから、その操作内容について個別かつ明示的な許可がない限り、以下を実行してはならない。

### Laravelの破壊的コマンド

```bash
php artisan migrate:fresh
php artisan migrate:refresh
php artisan db:wipe
php artisan schema:drop
php artisan migrate:reset
php artisan migrate:rollback --step=999
```

上記と同等の、既存テーブルまたは既存データを削除・再作成するコマンドも禁止する。

### SQLの破壊的操作

```sql
DROP DATABASE
DROP TABLE
TRUNCATE TABLE
DELETE FROM users
DELETE FROM migrations
```

次のような、WHERE条件のないDELETEを禁止する。

```sql
DELETE FROM table_name;
```

次のような、全件削除または大量削除につながる処理を禁止する。

```php
Model::query()->delete();
Model::truncate();
DB::table('table_name')->delete();
DB::table('table_name')->truncate();
```

### ファイルの破壊的操作

以下を禁止する。

* 既存ファイルの大量削除
* 既存ディレクトリの再作成
* 既存設定ファイルの全面置換
* 必要性を確認しないまま行うファイル削除
* 既存機能を一時的に削除して後から戻す実装
* バックアップを取らずに行う大規模変更

### Gitの破壊的操作

以下を禁止する。

```bash
git reset --hard
git clean -fd
git clean -fdx
git push --force
git push --force-with-lease
git rebase --onto
```

そのほか、既存の未コミット変更、Git履歴、ブランチ、タグを消失させる操作を禁止する。

---

## 3. 包括的な指示は削除許可ではない

以下のような指示は、破壊的操作の許可を意味しない。

* 完成させてください
* 自動で進めてください
* すべて実装してください
* エラーを直してください
* データベースを修正してください
* 必要な対応を行ってください
* Goalが完了するまで進めてください
* Codexの判断で進めてください

破壊的操作を行うには、ユーザーが削除対象と操作内容を明示した個別承認が必要である。

例えば、次のような明示が必要である。

```text
開発用のsaasdeck_codexデータベースについて、
既存データがすべて削除されることを理解したうえで、
php artisan migrate:freshの実行を許可します。
```

対象DB名、失われるデータ、実行するコマンドが明確でない許可は、破壊的操作の承認として扱わないこと。

---

## 4. データベース接続先の確認

マイグレーション、Seeder、自動テスト、データ更新、データ削除を行う前に、必ず接続先DBを確認すること。

次のSQLまたは同等の処理で確認する。

```sql
SELECT DATABASE();
```

Codexが操作してよいDBは、原則として以下のみとする。

```text
saasdeck_codex
saasdeck_test
```

以下のDBは、Codexから変更してはならない。

```text
saasdeck
本番環境のDB
他プロジェクトのDB
用途が不明なDB
```

接続先が不明な場合、または許可されたDB名と一致しない場合は、マイグレーション、Seeder、自動テスト、更新、削除を実行せず、作業を停止して報告すること。

DB名の一部が一致するだけでは許可しない。完全一致で確認すること。

---

## 5. rootユーザーの使用禁止

Codexの作業で、MySQLの`root`ユーザーを使用してはならない。

Codexは、対象DBだけに権限を制限した専用MySQLユーザーを使用すること。

Codex専用ユーザーには、次の権限を与えないこと。

* 他DBへのアクセス権限
* MySQLユーザーの作成・変更・削除権限
* グローバル権限
* DB全体の削除権限
* 権限付与権限
* 本番DBへのアクセス権限

次の操作は禁止する。

```sql
DROP USER
CREATE USER
ALTER USER
RENAME USER
GRANT
REVOKE
```

MySQLユーザーや権限の変更が必要な場合は、実行せず、必要なSQLと理由だけをユーザーへ提示すること。

---

## 6. マイグレーションのルール

既存マイグレーションを変更して、DBを作り直す前提の実装をしてはならない。

すでに適用済みのDB構造を変更する場合は、原則として新しいマイグレーションを追加すること。

既存テーブルやカラムを削除するマイグレーションは、ユーザーの個別承認なしに作成・実行してはならない。

次の操作が必要だと判断した場合は、実装を停止して報告すること。

* テーブル削除
* カラム削除
* カラム型の破壊的変更
* 外部キー削除
* 一意制約削除
* 既存データを失う可能性がある変更
* データ変換を伴う変更
* ロールバック不能な変更

報告には以下を含めること。

* 対象テーブル
* 対象カラム
* 変更理由
* 失われる可能性があるデータ
* 影響する機能
* 削除しない代替案
* バックアップ方法
* ロールバック方法

---

## 7. SeederとFactoryのルール

SeederやFactoryを実行する際に、既存データを削除してはならない。

以下のような処理をSeederへ含めることを禁止する。

```php
User::truncate();
SaasProject::truncate();
DB::table('users')->delete();
DB::statement('SET FOREIGN_KEY_CHECKS=0');
```

既存データがある環境でも安全に再実行できるよう、以下を使用すること。

* `firstOrCreate`
* `updateOrCreate`
* `upsert`
* 一意キーによる重複防止
* テスト専用DBへの限定

Seederの再実行によって、既存ユーザー、既存SaaS、既存設定、既存履歴が削除または上書きされないことを確認すること。

---

## 8. データ更新・削除前の確認

複数レコードの更新または削除を行う場合は、実行前に同じ条件でSELECTを行い、対象を確認すること。

最低限、以下を確認する。

* 対象テーブル
* WHERE条件
* 対象件数
* 対象レコードID
* 対象ユーザーID
* 他ユーザーへの影響
* 関連テーブルへの影響
* 復元方法
* ロールバック方法

削除件数が1件を超える可能性がある場合は、ユーザーの明示的な承認なしに実行してはならない。

条件が曖昧な削除を禁止する。

悪い例：

```sql
DELETE FROM users WHERE status = 'inactive';
```

より安全な例：

```sql
SELECT id, email, status
FROM users
WHERE id = 123;
```

対象を確認したうえで、明示的に指定された1件だけを操作すること。

---

## 9. usersテーブルの特別保護

`users`テーブルは最重要保護対象とする。

以下を禁止する。

* usersテーブルの全件削除
* usersテーブルのTRUNCATE
* usersテーブルの再作成
* usersテーブルを含むmigrate:fresh
* 条件が不十分なユーザー削除
* 認証方式変更に伴う既存ユーザー削除
* Seederによる既存ユーザーの置換
* テストデータ作成を理由とした既存ユーザー削除

ユーザー削除機能を実装する場合は、最低限以下を確認する。

* ログインユーザー自身か
* 管理者による操作か
* 対象IDが明示されているか
* 他ユーザーを削除しないか
* 関連データの扱いが定義されているか
* 論理削除を利用できないか
* 削除確認があるか
* 自動テストがあるか

ユーザー削除機能のテストでは、対象以外のユーザーが残ることを必ず確認すること。

---

## 10. 外部キー制約を無効化しない

問題解決のために外部キー制約を無効化してはならない。

以下を禁止する。

```sql
SET FOREIGN_KEY_CHECKS=0;
```

Laravelで同等の処理を行うことも禁止する。

外部キーエラーが発生した場合は、制約を無効にするのではなく、以下を調査すること。

* 親レコードの存在
* 子レコードの参照先
* 作成順序
* 削除順序
* user_id
* saas_project_id
* テストデータの整合性
* マイグレーションの順序

---

## 11. 自動テストの安全ルール

自動テストは、必ずテスト専用DBで実行すること。

テスト前に以下を確認する。

* `APP_ENV=testing`
* DB名が`saasdeck_test`または明示されたテスト専用DB
* 通常開発DBではない
* 本番DBではない
* 本物のStripeキーが設定されていない
* 実メールが送信されない
* 実課金が発生しない
* 本番QueueへJobを送信しない

`RefreshDatabase`、`DatabaseMigrations`、`DatabaseTransactions`など、DBを初期化または変更するテスト機能を使用する場合は、接続先がテスト専用DBであることを必ず確認すること。

接続先が`saasdeck`の場合、自動テストを実行してはならない。

---

## 12. ソースコード変更時のルール

既存機能を変更する前に、関連コードを調査すること。

最低限、以下を確認する。

* Controller
* Service
* Model
* Request
* Policy
* Resource
* Route
* React画面
* TypeScript型
* 自動テスト
* 関連マイグレーション
* 関連する別機能

修正範囲を必要最小限にすること。

別の問題を見つけても、現在の依頼と関係がない変更を無断で行わないこと。

大規模なリファクタリングと機能実装を同時に行わないこと。

既存ファイルを全面的に書き換える場合は、既存機能がすべて維持されていることを確認すること。

---

## 13. デグレード防止テスト

変更した機能だけでなく、関連する既存機能のテストも実行すること。

最低限、以下を確認する。

* ユーザー登録
* ログイン
* ログアウト
* 認証ユーザー取得
* SaaS一覧
* SaaS登録
* SaaS詳細
* SaaS編集
* SaaS削除
* 他ユーザーのデータ分離
* 既存ユーザー件数
* 既存SaaS件数

データ削除を伴わない機能変更では、処理前後で既存レコード件数が意図せず減っていないことを確認する。

例：

```php
$beforeUserCount = User::count();

$this->artisan('対象コマンド')
    ->assertSuccessful();

$this->assertSame($beforeUserCount, User::count());
```

削除機能のテストでは、対象レコードだけが削除され、それ以外のレコードが残ることを確認する。

---

## 14. バックアップの確認

DB構造変更、大量更新、データ移行を行う前に、バックアップの有無を確認すること。

バックアップの存在を確認できない場合、破壊的または大量変更の可能性がある作業を実行してはならない。

Codex自身が通常開発DBのバックアップを取得する必要はない。

バックアップが必要な場合は、実行せず、ユーザーへ次を報告すること。

* なぜバックアップが必要か
* 対象DB
* 実行前に確認すべき内容
* 推奨バックアップコマンド
* 復元方法

---

## 15. 問題発生時の対応順序

エラーやデータ不整合が発生した場合は、以下の順番で対応すること。

1. エラー内容を確認する
2. ログを確認する
3. 接続先DBを確認する
4. 関連コードを確認する
5. 関連データをSELECTで確認する
6. 原因を特定する
7. データを削除しない修正案を検討する
8. 修正前に自動テストを追加する
9. 最小限の修正を行う
10. 対象テストを実行する
11. 関連既存機能を再確認する
12. データ件数が意図せず減っていないことを確認する

原因不明のまま、DB初期化、テーブル再作成、ユーザー再作成で解決してはならない。

---

## 16. 作業を停止すべき条件

以下の場合は、勝手に判断して進めず、作業を停止して報告すること。

* 通常開発DBへ接続している
* 本番DBへ接続している可能性がある
* root権限しか利用できない
* 既存データ削除が必要に見える
* テーブル削除が必要に見える
* カラム削除が必要に見える
* usersテーブルへの大量操作が必要に見える
* 既存マイグレーションの書き換えが必要に見える
* Git履歴を書き換える必要がある
* 既存機能削除が必要に見える
* 影響範囲を特定できない
* ロールバック方法がない
* バックアップが確認できない
* ユーザーの指示と既存仕様が矛盾している

報告には以下を含めること。

* 現在の状況
* 実行しようとした操作
* なぜ危険なのか
* 影響範囲
* 安全な代替案
* ユーザーによる判断が必要な内容

---

## 17. 実装完了時の報告

作業完了時には、以下を報告すること。

### 変更内容

* 変更したファイル
* 追加したファイル
* 変更したDB構造
* 追加したAPI
* 変更した画面

### データ保護

* 使用したDB名
* データ削除の有無
* テーブル削除の有無
* カラム削除の有無
* 既存データへの影響
* usersテーブルへの影響
* 秘密情報への影響

### 検証

* 実行したテスト
* 成功件数
* 失敗件数
* ビルド結果
* Lint結果
* 既存機能確認結果
* デグレード確認結果

### 未解決事項

* 未実装
* 保留
* ブロック事項
* ユーザー確認が必要な事項

問題や失敗を隠して完了扱いにしてはならない。

---

## 18. 最優先原則

作業速度より、既存データと既存機能の保護を優先すること。

実装できない場合は、破壊して解決するのではなく、作業を停止して理由を報告すること。

迷った場合は実行しないこと。

削除するより残すこと。

初期化するより調査すること。

作り直すより修正すること。

自動実行するより確認すること。
